#include <bits/stdc++.h>
using namespace std; 

int check(int num) {
    
}


int main() {
    int n; 
    cin>>n;

    while(n--) {
        int alpha; 
        cin>> alpha;

        cout<<check(alpha)<<endl;
    }
}